platform: python
platformVersion: "3.12"
autoDeploy: true

run:
  port: 8000
  startCommand: bash /output/start.sh

database:
  enabled: true
  engine: postgres
  framework: raw
  schema: db/schema.sql

cache:
  enabled: true

blobs:
  enabled: true

healthCheck:
  path: /health
