# Next.js hybrid

One Next.js component emits both static assets and a standalone SSR server.
The component owns `/` and self-routes pages, dynamic content, and API routes.

```powershell
npm install
npm run build
npm start
```

The production server reads `PORT` and defaults to `3000`.

